/*!
 * Start Bootstrap - SB Admin 2 v4.0.7 (https://startbootstrap.com/template-overviews/sb-admin-2)
 * Copyright 2013-2019 Start Bootstrap
 * Licensed under MIT (https://github.com/BlackrockDigital/startbootstrap-sb-admin-2/blob/master/LICENSE)
 */

!function (t) { "use strict"; t("#sidebarToggle, #sidebarToggleTop").on("click", function (o) { t("body").toggleClass("sidebar-toggled"), t(".sidebar").toggleClass("toggled"), t(".sidebar").hasClass("toggled") && t(".sidebar .collapse").collapse("hide") }), t(window).resize(function () { t(window).width() < 768 && t(".sidebar .collapse").collapse("hide") }), t("body.fixed-nav .sidebar").on("mousewheel DOMMouseScroll wheel", function (o) { if (768 < t(window).width()) { var e = o.originalEvent, l = e.wheelDelta || -e.detail; this.scrollTop += 30 * (l < 0 ? 1 : -1), o.preventDefault() } }), t(document).on("scroll", function () { 100 < t(this).scrollTop() ? t(".scroll-to-top").fadeIn() : t(".scroll-to-top").fadeOut() }), t(document).on("click", "a.scroll-to-top", function (o) { var e = t(this); t("html, body").stop().animate({ scrollTop: t(e.attr("href")).offset().top }, 1e3, "easeInOutExpo"), o.preventDefault() }) }(jQuery);

/* AQUI */
/*

window.onload = function() {
  if (window.location.href.indexOf('entrar-evento.html') > -1) {
    showSlides();
  }
} */




/*ATÉ AQUI, TEM MAIS PARA A BAIXO TMB */

document.querySelectorAll('input[type=number]')
  .forEach(e => e.oninput = () => {
    // Always 2 digits
    if (e.value.length >= 2) e.value = e.value.slice(0, 2);
    // 0 on the left (doesn't work on FF)
    if (e.value.length === 1) e.value = '0' + e.value;
    // Avoiding letters on FF
    if (!e.value) e.value = '00';
  });



var dropdown = document.getElementsByClassName("dropdown-btn");
var i;

for (i = 0; i < dropdown.length; i++) {
  dropdown[i].addEventListener("click", function () {
    this.classList.toggle("active");
    var dropdownContent = this.nextElementSibling;
    if (dropdownContent.style.display === "block") {
      dropdownContent.style.display = "none";
    } else {
      dropdownContent.style.display = "block";
    }
  });
}


$(".myFilter").keyup(function () {
  val = $.trim(this.value);
  if (val === "") {
    $('.myfilter').show();
  } else {
    $('.myfilter').hide();
    $(".myfilter:contains(" + val + ")").show();
  }
});

$(function(){   

  $(".myFilter").keyup(function(){       

      var searchText = $(this).val().toUpperCase();

      $('.myfilter').each(function(){

          var currentLiText = $(this).text().toUpperCase(),
              showCurrentLi = currentLiText.indexOf(searchText) !== -1;
    
          $(this).toggle(showCurrentLi);

      });

  });

});



$(".myFilter2").keyup(function () {
  val = $.trim(this.value);
  if (val === "") {
    $('.myfilter2').show();
  } else {
    $('.myfilter2').hide();
    $(".myfilter2:contains(" + val + ")").show();
  }
});

$(function(){   

  $(".myFilter2").keyup(function(){       

      var searchText = $(this).val().toUpperCase();

      $('.myfilter2').each(function(){

          var currentLiText = $(this).text().toUpperCase(),
              showCurrentLi = currentLiText.indexOf(searchText) !== -1;
    
          $(this).toggle(showCurrentLi);

      });

  });

});



$(".myFilter3").keyup(function () {
  val = $.trim(this.value);
  if (val === "") {
    $('.myfilter3').show();
  } else {
    $('.myfilter3').hide();
    $(".myfilter3:contains(" + val + ")").show();
  }
});

$(function(){   

  $(".myFilter3").keyup(function(){       

      var searchText = $(this).val().toUpperCase();

      $('.myfilter3').each(function(){

          var currentLiText = $(this).text().toUpperCase(),
              showCurrentLi = currentLiText.indexOf(searchText) !== -1;
    
          $(this).toggle(showCurrentLi);

      });

  });

});



function walert() {
  alert("Campo alugado com Sucesso!!!");
}

//AQUI
//Imagens  --> Evento, Campo
	var slideIndex = 1;
	showSlides(slideIndex);

	function plusSlides(n) {
		  showSlides(slideIndex += n);
	}

	function currentSlide(n) {
		  showSlides(slideIndex = n);
	}


	function showSlides(n) {
		  var shi;
		  var slides = document.getElementsByClassName("mySlides");
		  var dots = document.getElementsByClassName("dot");
		  if (slides.length != 0) {
			if (n > slides.length) { slideIndex = 1 }
			  if (n < 1) { slideIndex = slides.length }
			  for (shi = 0; shi < slides.length; shi++) {
				slides[shi].style.display = "none";
			  }
			  for (shi = 0; shi < dots.length; shi++) {
				dots[shi].className = dots[shi].className.replace(" active", "");
			  }
			  slides[slideIndex - 1].style.display = "block";
			 dots[slideIndex - 1].className += " active";			
		} else {
		}
	}








// Participantes colapso --> Evento
	var coll = document.getElementsByClassName("collapsible");
	var i;

	for (i = 0; i < coll.length; i++) {
	  coll[i].addEventListener("click", function () {
		this.classList.toggle("active");
		var content = this.nextElementSibling;
		if (content.style.maxHeight) {
		  content.style.maxHeight = null;
		} else {
		  content.style.maxHeight = content.scrollHeight + "px";
		}
	  });
	}
//ATÉ AQUI

$(".myFilter4").keyup(function () {
  val = $.trim(this.value);
  if (val === "") {
    $('.myfilter4').show();
  } else {
    $('.myfilter4').hide();
    $(".myfilter4:contains(" + val + ")").show();
  }
});

$(function(){   

  $(".myFilter4").keyup(function(){       

      var searchText = $(this).val().toUpperCase();

      $('.myfilter4').each(function(){

          var currentLiText = $(this).text().toUpperCase(),
              showCurrentLi = currentLiText.indexOf(searchText) !== -1;
    
          $(this).toggle(showCurrentLi);

      });

  });

});



$(".myFilter5").keyup(function () {
  val = $.trim(this.value);
  if (val === "") {
    $('.myfilter5').show();
  } else {
    $('.myfilter5').hide();
    $(".myfilter5:contains(" + val + ")").show();
  }
});

$(function(){   

  $(".myFilter5").keyup(function(){       

      var searchText = $(this).val().toUpperCase();

      $('.myfilter5').each(function(){

          var currentLiText = $(this).text().toUpperCase(),
              showCurrentLi = currentLiText.indexOf(searchText) !== -1;
    
          $(this).toggle(showCurrentLi);

      });

  });

});



// ---------------------------------------------------Novo codigo--------------------------------------------------
